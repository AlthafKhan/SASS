var username ="Altaf";

function getUser(){
    return {
        "name": "khan",
        "loation" : "NJ" 
    }
}

var userObj = {
    "org" : "Marlabs",
    "designation": "frount end developer"
}
function loop_arr(arr, cb) {
    if(arr.length > 0){
        for(var i=0; i<arr.length; i++){
         cb('', arr[i]);   
        }                    
    }else {
       cb('Array is empty'); 
    }
}

function getUser(){
    var promise = new Promise (function(resolve, reject) {
    setTimeout(function(){
        resolve('getuser1() called!!');
    },3000);
    });
    return promise;
}

module.exports = {username, getUser, userObj, loop_arr, getUser}